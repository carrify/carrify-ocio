<div class="ad-list-content bds-{{category}}">
  {{#adverts}}
    <div class="col-100 ad-wrapper ad-small">
      <a href="javascript:void(0);" class="ad-content back-{{category}}" data-link-detail="{{@root.category}}" data-link-id="{{id}}" style="background-image:url('{{data.image}}');">
        <div class="ad-content-wrapper">
          {{{data.content.small}}}
        </div>
      </a>
    </div>
  {{/adverts}}
</div>
